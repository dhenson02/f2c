'use strict';

import Store from './store';

export const rosters = new Store();

export const liveScoring = new Store();

export const franchises = new Store();

export const league = new Store();

export const players = new Store();
